app.environment=dev
server.port=8081
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS}

logging.level.com.alon.bookstore=DEBUG

spring.datasource.url=${DB_URL:jdbc:postgresql://bookstore-dev-db:5432/bookstore_db}
spring.datasource.username=${DB_USERNAME:myuser}
spring.datasource.password=${DB_PASSWORD:secret}
